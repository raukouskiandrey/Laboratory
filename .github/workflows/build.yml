name: Build & SonarCloud

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

jobs:
  build-and-scan:
    runs-on: ubuntu-latest
    env:
      SONAR_ORG: raukouskiandrey
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Build Wrapper
        uses: SonarSource/sonarqube-scan-action/install-build-wrapper@v6

      - name: Build Laba1
        run: |
          rm -rf Laba1/build
          mkdir Laba1/build
          cmake -S Laba1 -B Laba1/build
          build-wrapper-linux-x86-64 --out-dir Laba1/build_wrapper make -C Laba1/build

      - name: SonarCloud scan Laba1
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=raukouskiandrey_Laba1
            -Dsonar.organization=${{ env.SONAR_ORG }}
            -Dsonar.sources=Laba1
            -Dsonar.cfamily.compile-commands=Laba1/build_wrapper/compile_commands.json
            -Dsonar.coverage.exclusions=**
            -Dsonar.cfamily.skipCoverage=true

      - name: Build Laba2
        run: |
          rm -rf Laba2/build
          mkdir Laba2/build
          cmake -S Laba2 -B Laba2/build
          build-wrapper-linux-x86-64 --out-dir Laba2/build_wrapper make -C Laba2/build

      - name: SonarCloud scan Laba2
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=raukouskiandrey_Laba2
            -Dsonar.organization=${{ env.SONAR_ORG }}
            -Dsonar.sources=Laba2
            -Dsonar.cfamily.compile-commands=Laba2/build_wrapper/compile_commands.json
            -Dsonar.coverage.exclusions=**
            -Dsonar.cfamily.skipCoverage=true

      - name: Build Laba3
        run: |
          rm -rf Laba3/build
          mkdir Laba3/build
          cmake -S Laba3 -B Laba3/build
          build-wrapper-linux-x86-64 --out-dir Laba3/build_wrapper make -C Laba3/build

      - name: SonarCloud scan Laba3
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=raukouskiandrey_Laba3
            -Dsonar.organization=${{ env.SONAR_ORG }}
            -Dsonar.sources=Laba3
            -Dsonar.cfamily.compile-commands=Laba3/build_wrapper/compile_commands.json
            -Dsonar.coverage.exclusions=**
            -Dsonar.cfamily.skipCoverage=true

      - name: Build Laba4
        run: |
          rm -rf Laba4/build
          mkdir Laba4/build
          cmake -S Laba4 -B Laba4/build
          build-wrapper-linux-x86-64 --out-dir Laba4/build_wrapper make -C Laba4/build

      - name: SonarCloud scan Laba4
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=raukouskiandrey_Laba4
            -Dsonar.organization=${{ env.SONAR_ORG }}
            -Dsonar.sources=Laba4
            -Dsonar.cfamily.compile-commands=Laba4/build_wrapper/compile_commands.json
            -Dsonar.coverage.exclusions=**
            -Dsonar.cfamily.skipCoverage=true

      - name: Build Laba5
        run: |
          rm -rf Laba5/build
          mkdir Laba5/build
          cmake -S Laba5 -B Laba5/build
          build-wrapper-linux-x86-64 --out-dir Laba5/build_wrapper make -C Laba5/build

      - name: SonarCloud scan Laba5
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=raukouskiandrey_Laba5
            -Dsonar.organization=${{ env.SONAR_ORG }}
            -Dsonar.sources=Laba5
            -Dsonar.cfamily.compile-commands=Laba5/build_wrapper/compile_commands.json
            -Dsonar.coverage.exclusions=**
            -Dsonar.cfamily.skipCoverage=true

      - name: Build Laba6
        run: |
          rm -rf Laba6/build
          mkdir Laba6/build
          cmake -S Laba6 -B Laba6/build
          build-wrapper-linux-x86-64 --out-dir Laba6/build_wrapper make -C Laba6/build

      - name: SonarCloud scan Laba6
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=raukouskiandrey_Laba6
            -Dsonar.organization=${{ env.SONAR_ORG }}
            -Dsonar.sources=Laba6
            -Dsonar.cfamily.compile-commands=Laba6/build_wrapper/compile_commands.json
            -Dsonar.coverage.exclusions=**
            -Dsonar.cfamily.skipCoverage=true